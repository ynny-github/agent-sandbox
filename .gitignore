# common
tmp/

# stray local build output, if anyone still builds here by hand; `mise run
# build` itself installs to GOPATH/bin now, not here (see .mise.toml)
/bin/

# python
.venv/
.pytest_cache/
__pycache__/
*.pyc

# javascript / typescript
node_modules/

# claude
.claude/settings.local.json

# superpowers scratch (ledgers, task briefs, review packages)
.superpowers/
